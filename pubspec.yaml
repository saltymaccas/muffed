name: muffed
description: A lemmy client written in flutter

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.5.0+5

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  any_link_preview: ^3.0.1
  equatable: ^2.0.5
  flutter_bloc: ^8.1.3
  dynamic_color: ^1.6.5
  dio: ^5.3.0
  hydrated_bloc: ^9.1.2
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  go_router: ^11.0.0
  http: ^1.1.0
  url_launcher: ^6.1.11
  video_player: ^2.7.0
  bloc_concurrency: ^0.2.2
  flutter_animate: ^4.2.0
  flutter_markdown: ^0.6.17
  path_provider: ^2.0.15
  cached_network_image: ^3.2.3
  flutter_colorpicker: ^1.0.3
  logging: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  very_good_analysis: ^5.0.0+1

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/logo.svg
    - assets/placeholder_banner.jpeg